# 📖 คู่มือการติดตั้งโปรเจกต์และการซิงค์ข้อมูลข้ามเครื่อง (Multi-device Sync Guide)
จัดทำขึ้นเพื่อช่วยให้คุณครูสามารถนำระบบฐานข้อมูลนักเรียนและคัดกรองความเสี่ยงนี้ ไปรันและเปิดพัฒนาต่อบน **เครื่องโน้ตบุ๊กอีกเครื่อง** ได้อย่างสะดวก รวดเร็ว และข้อมูลตรงกันเสมอร้อยเปอร์เซ็นต์

---

## 💡 แนวคิดการซิงค์ข้อมูลของโปรเจกต์นี้ (Sync Strategy)

โปรเจกต์นี้แยกส่วนการทำงานออกเป็น 2 ส่วนหลัก ซึ่งซิงค์ตรงกันผ่านระบบคลาวด์แบบอัตโนมัติอยู่แล้ว:
1. **โค้ดหน้าบ้าน (หน้าเว็บ/ดีไซน์/ปุ่มกด)**: จัดเก็บอยู่ในโฟลเดอร์ `G:\My Drive\student-database` ซึ่งทำงานร่วมกับ **Google Drive Desktop** ทำให้ไฟล์แก้ไขล่าสุดซิงค์ข้ามเครื่องโดยอัตโนมัติ
2. **ฐานข้อมูลหลังบ้าน (รายชื่อและประวัตินักเรียน 76 คน)**: จัดเก็บอยู่บน **Supabase Cloud (PostgreSQL)** ซึ่งออนไลน์ตลอดเวลา ไม่ว่าคุณครูจะเปิดระบบจากเครื่องใด ข้อมูลจะถูกดึงมาซิงค์สดเรียลไทม์เป็นชุดเดียวกันเสมอ

---

## 💻 ขั้นตอนการตั้งค่าโน้ตบุ๊กเครื่องใหม่ (Step-by-Step Setup)

รบกวนคุณครูดำเนินการตาม 3 ขั้นตอนง่ายๆ ดังนี้ในเครื่องโน้ตบุ๊กใหม่ครับ:

### 📥 ขั้นตอนที่ 1: ติดตั้ง Google Drive Desktop (เพื่อดึงไฟล์โค้ด)
1. ดาวน์โหลดและติดตั้งโปรแกรม **Google Drive Desktop** บนเครื่องโน้ตบุ๊กใหม่
2. ลงชื่อเข้าใช้งานด้วย **บัญชี Google บัญชีเดียวกับเครื่องหลัก**
3. โปรแกรมจะทำการจำลองไดรฟ์ขึ้นมาในเครื่องโน้ตบุ๊ก (ปกติจะเป็นไดรฟ์ `G:\`)
4. ให้เปิดดูที่พาธ `G:\My Drive\student-database` คุณครูจะพบไฟล์ทั้งหมดปรากฏอยู่ทันทีเหมือนในเครื่องเดิมครับ

---

### 🛠️ ขั้นตอนที่ 2: ติดตั้งเครื่องมือเขียนโค้ดและรันจำลองเว็บ (VS Code & Live Server)
ในการเปิดรันหน้าเว็บทดสอบในเครื่องโน้ตบุ๊ก แนะนำให้ใช้เครื่องมือมาตรฐานดังนี้ครับ:
1. ดาวน์โหลดและติดตั้งโปรแกรมเขียนโค้ด **Visual Studio Code (VS Code)**
2. เปิด VS Code ขึ้นมา -> เลือกเมนู **File** -> **Open Folder** -> เลือกโฟลเดอร์ `G:\My Drive\student-database`
3. ไปที่แถบ **Extensions** (ไอคอนกล่องสี่เหลี่ยมด้านซ้าย) -> ค้นหาคำว่า **Live Server** (พัฒนาโดย Ritwick Dey) -> กด **Install**
4. **วิธีการเปิดรันหน้าเว็บทดสอบ**:
   - คลิกเปิดไฟล์ `index.html` ใน VS Code
   - กดปุ่ม **Go Live** ที่มุมขวาล่างของโปรแกรม
   - หน้าเว็บฐานข้อมูลจะเปิดขึ้นบนบราวเซอร์ของเครื่องโน้ตบุ๊กใหม่ทันที และพร้อมใช้งานเชื่อมต่อกับ Supabase คลาวด์จริง

---

### 🌐 ขั้นตอนที่ 3: ตั้งค่าระบบ Git สำหรับอัปโหลดขึ้นเว็บจริง (GitHub Pages)
หากคุณครูทำการแก้ไขโค้ดหน้าตาเว็บในโน้ตบุ๊ก และต้องการอัปโหลดขึ้นออนไลน์ เพื่อให้สามารถใช้งานจากมือถือหรือหน้าเว็บสาธารณะได้เหมือนเดิม ให้ตั้งค่า Git ดังนี้ครับ:
1. ดาวน์โหลดและติดตั้ง **Git** บนโน้ตบุ๊กใหม่: [https://git-scm.com/downloads](https://git-scm.com/downloads)
2. ดาวน์โหลดโปรแกรมช่วยจัดการแบบหน้าต่าง **GitHub Desktop** เพื่อความง่าย: [https://desktop.github.com](https://desktop.github.com)
3. เปิด GitHub Desktop -> ลงชื่อเข้าใช้บัญชี GitHub ของคุณครู
4. เลือกเมนู **File** -> **Add Local Repository** -> กด **Browse** เลือกโฟลเดอร์ `G:\My Drive\student-database` -> กด **Add Repository**
5. **วิธีการส่งไฟล์ขึ้นออนไลน์เมื่อแก้ไขเสร็จ**:
   - เมื่อแก้โค้ดเสร็จ ในโปรแกรม GitHub Desktop จะแสดงไฟล์ที่เปลี่ยนไป
   - ให้พิมพ์ข้อความในช่อง Summary ด้านซ้ายล่าง (เช่น "ปรับแก้ฟิลเตอร์") แล้วกดปุ่ม **Commit to main**
   - จากนั้นกดปุ่ม **Push origin** ที่บาร์ด้านบน โค้ดใหม่จะถูกอัปโหลดขึ้น GitHub Pages ของคุณครูจริงทันทีภายใน 1 นาทีครับ!

---

## 🗄️ ข้อมูลอ้างอิงของคลาวด์หลังบ้าน (Supabase Connection Details)
ข้อมูลนี้ถูกใส่ไว้ในไฟล์ `app.js` ของโครงการเรียบร้อยแล้ว ไม่จำเป็นต้องกรอกซ้ำ แต่บันทึกไว้เพื่อความปลอดภัยครับ:
* **Supabase Project URL**: `https://iapcotozckbhpcmyzoqd.supabase.co`
* **Anon Public API Key**: `sb_publishable_Ylh3TU3WMMxcyGB7C6a-jA_mTZ0QavA`
* **ชื่อตารางหลัก**: `students` (ประกอบด้วยโครงสร้างข้อมูลประวัติ 35 คอลัมน์)

---

## 💾 ขั้นตอนการสำรองและกู้คืนข้อมูลแบบเร่งด่วน (JSON Backup / Restore)
คุณครูสามารถทำการแบ็คอัพเก็บไว้เป็นเอกสารออฟไลน์เพื่อความปลอดภัยทางข้อมูลสูงสุดได้ตลอดเวลา:
1. **การดาวน์โหลดสำรองข้อมูล**:
   - เปิดหน้าโปรแกรมเว็บสารสนเทศนักเรียน -> ไปที่หน้า **"ตั้งค่าระบบ" (Settings)**
   - ในหัวข้อ "บำรุงรักษาและสำรองข้อมูลระบบ" ให้กดปุ่ม **"ดาวน์โหลด JSON"**
   - ระบบจะดาวน์โหลดไฟล์ประวัตินักเรียนทั้งหมด 76 คน ณ เวลานั้น ออกมาเป็นไฟล์ `.json`
   - แนะนำให้นำไฟล์นี้ไปเซฟเก็บไว้ในโฟลเดอร์ `G:\My Drive\student-database\data_backup`
2. **การกู้คืนข้อมูล**:
   - หากเกิดกรณีฉุกเฉินข้อมูลในคลาวด์หาย คุณครูสามารถเข้าไปที่หน้า **"ตั้งค่าระบบ"**
   - กดปุ่ม **"อัปโหลดไฟล์กู้คืน"** -> เลือกไฟล์ JSON ที่เซฟเก็บไว้ในโฟลเดอร์ `data_backup`
   - ระบบจะเขียนทับข้อมูลและทำการเชื่อมข้อมูลกลับสู่ Supabase ให้ทันทีครับ!
